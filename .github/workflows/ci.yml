name: CI

on:
  push:
    branches: [ main ]
  pull_request:

jobs:
  api-contract:
    name: API Contract
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: pip
          cache-dependency-path: backend/requirements.txt

      - run: pip install -r backend/requirements.txt

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: src/package-lock.json

      - run: npm ci
        working-directory: src

      - run: npm run codegen:check
        working-directory: src

      - run: npm run typecheck:contract
        working-directory: src

  backend:
    name: Backend (Server / backend)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: pip
          cache-dependency-path: backend/requirements.txt

      - run: pip install -r requirements.txt

      - name: Lint
        run: ruff check .

      - name: Test
        run: pytest tests/ -v

  frontend:
    name: Frontend (React / Vercel)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: src
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: src/package-lock.json

      - run: npm ci

      - name: Test
        run: npm test

      - name: Typecheck app
        run: npx tsc --project tsconfig.app.json --noEmit

      - name: Typecheck Vercel middleware
        run: npx tsc --project tsconfig.node.json --noEmit

      - name: Lint
        run: npm run lint

      - name: Build
        run: npm run build
